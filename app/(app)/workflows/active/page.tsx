'use client';

/**
 * Active Workflows page - Displays currently running and paused workflows.
 *
 * Serves as the sidebar navigation destination for viewing active workflows.
 * This is a placeholder page that will be enhanced with workflow list and
 * filtering functionality in future iterations.
 */
export default function ActiveWorkflowsPage() {
  return (
    <main aria-label={'Active Workflows'} className={'space-y-6'}>
      {/* Page Header */}
      <h1 className={'text-2xl font-semibold tracking-tight'}>Active Workflows</h1>
    </main>
  );
}
