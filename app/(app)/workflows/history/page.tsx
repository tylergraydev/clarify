'use client';

/**
 * Workflow History page - Displays completed and archived workflows.
 *
 * Serves as the sidebar navigation destination for viewing workflow history.
 * This is a placeholder page that will be enhanced with workflow history list,
 * filtering, and search functionality in future iterations.
 */
export default function WorkflowHistoryPage() {
  return (
    <main aria-label={'Workflow History'} className={'space-y-6'}>
      {/* Page Header */}
      <h1 className={'text-2xl font-semibold tracking-tight'}>Workflow History</h1>
    </main>
  );
}
